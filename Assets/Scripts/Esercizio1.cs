using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Esercizio1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello World"); //Il messaggio in console viene mostrato una sola volta
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Hello World"); //Il messaggio viene mostrato una volta per ogni frame dell'esecuzione
    }
}
